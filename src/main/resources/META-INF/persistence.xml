<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="blogPU">

        <!-- (Optional) Чётко укажите поставщика Hibernate. -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entity -->
        <class>org.example.blog.model.User</class>
        <class>org.example.blog.model.Topic</class>
        <class>org.example.blog.model.Post</class>
        <class>org.example.blog.model.Comment</class>
        <class>org.example.blog.model.SavedPost</class>
        <class>org.example.blog.model.PostView</class>

        <properties>
            <!-- PostgreSQL -->
            <property name="javax.persistence.jdbc.driver"
                      value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url"
                      value="jdbc:postgresql://localhost:5432/blog_system"/>
            <property name="javax.persistence.jdbc.user"
                      value="postgres"/>
            <property name="javax.persistence.jdbc.password"
                      value="postgres"/>

            <!-- Hibernate options -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Создание/обновление таблиц автоматически на этапе разработки. -->
            <property name="hibernate.hbm2ddl.auto"
                      value="update"/>

            <property name="hibernate.show_sql"
                      value="true"/>
            <property name="hibernate.format_sql"
                      value="true"/>

            <!-- (Optional) Уменьшите количество ошибок отложенной загрузки при привязке данных к JavaFX -->
            <property name="hibernate.enable_lazy_load_no_trans"
                      value="true"/>

            <!-- (Optional) Стабилизировать часовой пояс для временной метки. -->
            <property name="hibernate.jdbc.time_zone"
                      value="UTC"/>
        </properties>
    </persistence-unit>

</persistence>
